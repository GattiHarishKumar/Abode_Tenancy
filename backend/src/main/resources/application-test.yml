spring:
  datasource:
    url: jdbc:h2:mem:abodedb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  flyway:
    enabled: false # H2 will generate DDL directly from JPA in unit tests for speed, while Flyway tests run against Postgres
  data:
    redis:
      host: localhost
      port: 6379

security:
  jwt:
    secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
    access-token-expiration-ms: 3600000
    refresh-token-expiration-ms: 86400000
  otp:
    mock-enabled: true
    default-otp: "123456"

storage:
  type: local
  local:
    base-path: ./target/test-uploads

logging:
  level:
    root: WARN
    com.abode.tenancy: INFO

